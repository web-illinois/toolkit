import { css } from 'lit';

export default css`

section {
  position: relative;
  margin: 0;
  padding: 0;
}
.content {
  max-width: var(--il-layout-max-width);
  margin-left: auto;
  margin-right: auto;
}

`;