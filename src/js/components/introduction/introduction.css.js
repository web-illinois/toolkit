import { css } from 'lit';

export default css`
.heading {
    font-size: 2.25rem;
    line-height: 2.625rem;
    font-style: oblique;
    margin-bottom: 0.9375rem;
    max-width: var(--il-content-max-width);
    margin: 0 auto;
}

.introduction {
    font-size: 1.375rem;
    line-height: 1.75rem;
    margin-bottom: 0.9375rem;
    max-width: var(--il-content-max-width);
    margin: 0 auto;
}
`;
