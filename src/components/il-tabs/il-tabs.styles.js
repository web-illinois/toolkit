import { css } from 'lit';

export default css `
    #container {
        display: var(--il-tabs--display);
    }
    #tabpanels {
        border: thin solid var(--il-blue);
        flex: 1 0 auto;
    }


`;